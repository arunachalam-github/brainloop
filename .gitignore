# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/
*.egg-info/
dist/

# PyInstaller build artifacts (build/brainloopd.spec is the only tracked file in build/)
build/venv/
build/dist/
build/build/
build/*.toc

# Tauri-embedded resources are regenerated by `make bundle-daemon`
app/src-tauri/resources/

# macOS
.DS_Store
.AppleDouble
._*
.Spotlight-V100
.Trashes

# brainloop runtime artifacts — user-specific, never commit
com.brainloop.agent.plist
*.db
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env

# Git worktrees (local isolation; never commit)
.worktrees/
